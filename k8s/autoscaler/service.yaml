apiVersion: v1
kind: Service
metadata:
  name: rl-autoscaler-service
  namespace: rl-autoscaler
  labels:
    app: rl-autoscaler
spec:
  selector:
    app: rl-autoscaler
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
  type: ClusterIP
